name: Crusher Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
 
jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install -g crusher-cli@1.3.25

#     - name: Deploy to Heroku
#       env:
#         HEROKU_API_TOKEN: ${{ secrets.HEROKU_API_TOKEN }}
#         HEROKU_APP_NAME: "crusher-test"
#       run: git push https://heroku:4061a111-a6e1-45ca-b299-2bfa71b94ac1@git.heroku.com/crusher-test.git master

    - name: RUN CLI Checks
      run: npx crusher-cli run --project_id=40  --crusher_token=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjo0NSwidGVhbV9pZCI6MzQsImlhdCI6MTU5NTIxMzQ0MywiZXhwIjo1MTk1MjA5ODQzfQ.VXPeJCGnH-ISGPHkinbwqKo3DEnoH_zvtJeGElQAvH4
